apiVersion: v2
name: postfix
description: A HELM chart for deploying Postfix/Dovecot

type: application

# Chart version does not reflect the Postfix version
version: 10.0.1

# General Postfix version this chart works with
appVersion: 3.5.0

